# 저장소 문체·표기 규약

이 문서는 `computer-science-interview-KR`의 문서 245편을 검수·집필할 때 따르는 규약입니다.
새 문서를 쓸 때도, 기존 문서를 고칠 때도 이 기준을 따릅니다. 이 규약과 실제 문서가
어긋나는 곳을 기계적으로 찾아내는 도구는 `tools/docs-studio/scripts/lint-style.js`이며,
`npm run lint`로 돌립니다.

## 1. 문체 — 합니다체 단일 규칙

이 저장소의 모든 마크다운 문서는 **합니다체**로 씁니다. 문서 종류나 절에 따라
말투를 나누지 않습니다. 평서형 종결은 `~합니다`, `~입니다`, `~됩니다`, `~습니다`로
끝냅니다. 독자에게 직접 지시하는 문장에는 `~하세요`, `~하십시오`를 쓰며, 이것도
합니다체의 일부로 봅니다.

| 규약 이전 | 규약 이후 | 예 |
|---|---|---|
| `~한다` `~하다` | `~합니다` | "컨텍스트 스위칭은 커널이 CPU 제어권을 넘기는 과정을 가리킨다" → "…가리킵니다" |
| `~된다` | `~됩니다` | "캐시가 무효화된다" → "캐시가 무효화됩니다" |
| `~이다`, 명사 + `~다` | `~입니다` | "기밀성, 무결성, 인증 세 가지이다" → "…세 가지입니다" |
| `~ㄴ다` (쓴다·둔다·만든다류) | `~ㅂ니다` (씁니다·둡니다·만듭니다) | "URL에 둔다" → "URL에 둡니다" |
| `~는다` (읽는다·않는다류) | `~습니다` | "값을 읽는다" → "값을 읽습니다" |
| 자음 어간 (높다·많다·어렵다류) | `~습니다` | "오버헤드가 크다" → "오버헤드가 큽니다" |
| 과거 `~았/었/했다` | `~았/었/했습니다` | "캐시에 올렸다" → "캐시에 올렸습니다" |

### 1.1 이 규약이 적용되지 않는 자리

- **코드 펜스 안**과 그 안의 한글 주석
- **HTML 주석** — 다이어그램 마커, 보존용 ASCII
- **인용부호 안** — `"..."`, `「...」`, `『...』`로 감싼 다른 화자의 말
- **헤딩** — 제목은 명사형이든 `~한다`든 그대로 둡니다
- **체크리스트 항목** — `- [ ] ~할 수 있다`는 독자가 스스로에게 묻는 자가 진단
  문장이므로 한다체를 유지합니다
- **표 셀** — 명사구로 씁니다
- **SVG 다이어그램 안의 라벨 텍스트** — 그림 캡션은 전보문체이므로 본문 규약 밖입니다.
  본문 산문·보존 ASCII와 달리 SVG 라벨은 폭 880 고정 레이아웃에 수동으로 배치되어
  있어, 종결어미를 늘리면 글자가 겹치거나 삐져나옵니다

### 1.2 검사

`tools/docs-studio/scripts/lint-style.js`의 `register-plain-leak` 규칙이 문서 245편
전체에서 마침표로 끝나는 해라체 종결을 오류로 잡습니다. 절 구분도, 파일 종류별
예외도 없습니다.

## 2. 엠대시(—) — 유지

엠대시는 삽입구·부연·대조를 표시하는 저장소 관례로 **의도적으로 채택되어 있습니다**
(문서 155편에 1,562회). 콜론이나 접속사로 기계적으로 치환하지 않습니다. 새 글을 쓸
때도 같은 방식으로 자유롭게 씁니다. 린터는 엠대시를 검사 대상에 넣지 않습니다.

AI 윤문 도구를 돌릴 때는 "대시로 이어진 부연을 쉼표·괄호·별도 문장으로 분해한다"는
류의 규칙을 끄고 실행합니다. 이 저장소는 파일당 평균 10회꼴로 엠대시를 쓰므로,
그런 규칙이 켜져 있으면 발동 조건에 쉽게 걸립니다.

## 3. 표기 표준

같은 개념을 문서마다 다르게 쓰면 검색과 학습에 방해가 됩니다. 아래 오른쪽 표기로
통일합니다.

| 비표준 | 표준 | 비고 |
|--------|------|------|
| 디렉토리 | **디렉터리** | 국립국어원 외래어 표기법 기준 |
| 쓰레드 | **스레드** | |
| 메세지 | **메시지** | |
| 트랜색션 | **트랜잭션** | |
| 어플리케이션 | **애플리케이션** | |
| 컨텐츠 | **콘텐츠** | |

영문 병기는 `한글(English)` 순서를 기본으로 합니다. 예: `트랜잭션(Transaction)`.
이미 굳어진 통용 축약어(`DB`, `API`, `URL` 등)는 그대로 씁니다.

## 4. 문서 골격

### 4.1 개념 설명 문서 (`NN-*.md`)

1. 학습 목표 / 선행 지식
2. 왜 필요한가
3. 동작 원리
4. 실무에서는
5. 면접 포인트
6. 자주 하는 실수
7. 한 줄 정리 / 연관 개념

시간이 없는 독자는 1 → 5 → 6만 보아도 되도록, 이 세 절은 어떤 문서에서도 생략하지
않습니다. 2번 「왜 필요한가」는 나머지 6단이 갖춰졌다면 원칙적으로 포함하되, 개념 자체가
필요성 논증보다 절차 서술에 가까운 문서(예: 트러블슈팅 가이드, 실전 시나리오)는 생략할
수 있습니다.

### 4.2 QnA 문서 (`qna-*.md`)

- 머리말에 사용법 안내(답을 먼저 가리고 풀어본다, 확인 후 복습한다 등)
- `## QN. 질문` 형식의 번호 붙은 절
- ⭐ 표시의 의미를 머리말에 명시 (아래 5절 참고)

### 4.3 폴더 README

- 그 주제를 배우는 순서(지도) 안내
- 하위 문서 목록 표
- 학습 체크리스트 또는 다음 주제로의 연결

## 5. ⭐ 기호의 의미

⭐는 파일 종류에 따라 뜻이 다릅니다. **혼동을 피하기 위해 각 문서 머리말에 그 문서에서
⭐가 무엇을 뜻하는지 명시**합니다.

- `qna-*.md`: ⭐는 **빈출 질문** 표시
- 폴더 README의 문서 목록 표: ⭐는 **난이도** 표시 (⭐ 1개 = 낮음, ⭐⭐⭐ = 높음)

두 의미를 한 문서 안에서 섞어 쓰지 않습니다. 난이도를 「전공자 학년 기준」
(1~2학년 / 3~4학년 / 주니어 현업)으로 서술하는 것은 폴더 README와 루트 README의
학습 체크리스트에서만 쓰고, 개념 문서 본문에는 넣지 않습니다.

⭐와 함께 쓰이는 ✅·❎도 의미를 지닌 표기입니다(자가 진단 체크리스트의 완료·미완료
표시). AI 윤문 도구를 돌릴 때는 이런 기호를 장식 이모지로 오인해 걷어내지 않도록
지시합니다.

## 6. 다이어그램 규약

시간축·상태 전이·계층 중첩처럼 글과 ASCII로 잘 전달되지 않는 자리에는 SVG 다이어그램을
씁니다. 삽입 지점은 항상 아래 세 부분으로 구성된 하나의 세트입니다.

```
<!-- diagram:고유-id -->
![대체 텍스트](../../assets/diagrams/고유-id.svg)

<!-- (여기에는 이 그림이 대신하게 된, 예전에 쓰던 텍스트 그림을 그대로 보존해 둔다.
     내용을 고칠 때는 그림도 함께 갱신할 것.)
```(예전 텍스트 그림)```
-->
```

> 실제 문서에서 이 보존용 주석이 정확히 어떤 문구로 시작하는지는 기존 문서 아무 곳이나
> 열어서 확인하세요. 이 규약 문서는 예시이므로 그 문구를 글자 그대로 옮기지 않습니다 —
> 코퍼스 검사 스크립트가 "보존용 주석이 본문 렌더링 결과로 새어 나왔는지"를 바로 그
> 문구의 등장 여부로 판정하기 때문에, 여기서 그대로 인용하면 이 규약 문서 자체가
> 오탐으로 걸립니다.

**개념을 수정해서 다이어그램의 내용이 달라지면 세 곳을 모두 갱신합니다.**

1. 산문 설명
2. `assets/diagrams/고유-id.svg`
3. 위 세트 안의 보존용 ASCII

SVG 자체는 `tools/docs-studio/server/validate.js`가 강제하는 다음 규칙을 만족해야
합니다.

- `viewBox`는 `0 0 880 H` 형태 (폭 880 고정)
- 한글 폰트 폴백(`Malgun Gothic`, `Apple SD Gothic Neo`, `Noto Sans KR` 중 하나)을
  `font-family`에 명시
- `<title>`과 `<desc>`를 모두 채움
- 전체를 덮는 흰 배경 `<rect fill="#ffffff">` 포함 (다크 테마에서 글자가 사라지는 것을 방지)
- `<script>`, `<style>`, `<foreignObject>`, `<iframe>`, `<a>` 등 GitHub이 걸러내는 태그와
  외부 리소스 참조, 이벤트 핸들러 속성 금지

다이어그램을 추가하거나 삭제하면 `tools/docs-studio/scripts/check-corpus.js`의
`EXPECT` 상수(문서 수·다이어그램 수·보존 ASCII 수)도 함께 갱신합니다.

## 7. 파일 규약

- 인코딩: UTF-8, BOM 없음
- 개행: CRLF (`core.autocrlf=true` 환경 기준)
- 파일명: `NN-kebab-case.md` (개념 설명), `qna-주제.md` (QnA), `README.md` (폴더 지도)
- 상대 링크만 사용하고, 저장소 밖을 가리키는 링크를 새로 만들지 않음

## 8. 피해야 할 표현

아래는 금지가 아니라 **과용을 피하는 기준**입니다. 문장을 자연스럽게 만드는 데 필요하면
씁니다. 다만 같은 표현이 한 문서에 반복되어 기계적으로 느껴진다면 다듬습니다.

- `~에 대한`, `~를 통해`의 남용 — 번역투로 읽히기 쉬운 자리이므로, 구체적인 동사나
  조사로 바꿀 수 있는지 먼저 살핍니다.
- 불필요한 `~들` — 이미 복수임이 문맥상 분명한 명사에 `~들`을 붙이지 않습니다.
  (`데이터들` → `데이터`, `정보들` → `정보`)
- `핵심은 ~이다` 류의 상투적 도입 — 매 절마다 반복되면 기계적으로 읽힙니다. 다른 방식의
  도입 문장과 섞어 씁니다.
- 사실 관계가 시간에 따라 달라지는 서술(특정 버전 번호, "20xx년 기준", 특정 시점의
  가격·한도)은 근거 없이 단정하지 않습니다. 근거가 필요하면 각주나 링크로 출처를 남깁니다.

## 9. 이 규약이 다루지 않는 것

사실관계의 정확성(개념 설명이 기술적으로 옳은가)은 이 문서의 범위가 아니라 별도의
내용 검수 절차에서 다룹니다. 이 문서는 문체·표기·구조에 대한 규약입니다.

이 문서가 규정하는 문체(1절)와 표기(3절)는 **마크다운 문서에만** 적용됩니다.
커밋 메시지 본문과 `tools/` 아래 소스 코드의 주석은 이 저장소의 별개 관례(한다체,
구체적 수치 명시)를 따르며 이 규약의 대상이 아닙니다.
